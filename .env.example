# Application environment: 'local' or 'production'
APP_ENV=local

# HTTP server
SERVER_PORT=8080
BASE_URL=http://localhost:8080

# Optional timeouts (duration format: 10s, 1m, 1h)
# SERVER_READ_TIMEOUT=10s
# SERVER_WRITE_TIMEOUT=15s
# SERVER_IDLE_TIMEOUT=60s
# SERVER_SHUTDOWN_TIMEOUT=10s

# Database
DATABASE_URL=postgres://root:secret@localhost:5432/url_shortener?sslmode=disable

# Redis
REDIS_URL=redis://localhost:6379/0

# JWT — secret must be at least 32 bytes (HS256 requirement)
JWT_SECRET=PgK13YiT0UpoxxpbusZJbnm85yNBEmdfcPCL4GLIxkT
# Duration format: number + unit (s, m, h)
JWT_TTL=24h

# CORS — comma-separated list of allowed origins
CORS_ORIGINS=http://localhost:5173