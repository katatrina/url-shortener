# Normalize line endings: LF in the repo and working tree on every platform.
# Overrides each machine's core.autocrlf, so no CRLF creeps in from Windows.
* text=auto eol=lf

# Binary files: never touch their bytes.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
